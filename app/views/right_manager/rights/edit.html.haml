.page-header
  = link_to rights_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    = t('view.right_manager.list')

  %h1= RightManager::Right.model_name.human + '-' + t('view.right_manager.edit')

= render 'form'
